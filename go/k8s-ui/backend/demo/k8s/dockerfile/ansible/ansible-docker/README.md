## Ansible manage multiple hosts(mock by docker container) 

```bash
./apply.sh
```


## Ansible manage Docker container

```bash
pip3 install 'docker-py>=1.7.0'
pip3 install 'docker-compose>=1.7.0'
```
